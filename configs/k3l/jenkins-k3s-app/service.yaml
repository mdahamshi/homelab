# jenkins-k3s-app — Service (namespace: default, NodePort, port 80 -> container 3000)
apiVersion: v1
kind: Service
metadata:
  name: jenkins-k3s-app
  namespace: default
spec:
  type: NodePort
  selector:
    app: jenkins-k3s-app
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
