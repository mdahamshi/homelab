# Cloudflared (home tunnel) — environment (placeholders only — copy to .env)
# Create the tunnel in the Cloudflare Zero Trust dashboard and paste its token.
SERVICE_NAME_CLOUDFLARED=cloudflared
CLOUDFLARE_TUNNEL_TOKEN=CHANGE_ME_TUNNEL_TOKEN
TUNNEL_TOKEN=CHANGE_ME_TUNNEL_TOKEN
